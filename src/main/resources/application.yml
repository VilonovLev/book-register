application:
  max-allowed-books: 2
server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book_registry?createDatabaseIfNotExist=true
    username: root
    password: 11235
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

